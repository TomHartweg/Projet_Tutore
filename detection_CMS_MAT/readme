#makefile:
  #make all = compile tous les fichires non compilés puis le "events2.cpp"
  #make clean = delete tous les .o

#fichier d'entree:
#"parametre.in" : syntaxe (R,H,DX,DY,DZ,X,Y,Z,epsilon,ctaumin,ctaumax,ctaupas)

#fichier a executer:
#"events2.cpp" g++ -o events2.exe events2.cpp
